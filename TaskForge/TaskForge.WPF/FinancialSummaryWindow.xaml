<Window x:Class="TaskForge.WPF.FinancialSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Financial Summary" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Financial summary" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="List" FontSize="18" FontWeight="Medium" Margin="0,10,0,10"/>

                <ListView x:Name="ExpensesListView"
                          Background="White"
                          BorderThickness="0"
                          HorizontalContentAlignment="Stretch">
                    <ListView.Resources>
                        <Style TargetType="TextBlock" x:Key="AmountStyle">
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="#D32F2F"/>
                            <Setter Property="Text" Value="{Binding Amount, StringFormat='-{0:F2}$'}"/>
                        </Style>
                    </ListView.Resources>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Description}" FontSize="16" FontWeight="Bold"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="{Binding Date, StringFormat={}{0:dd/MM}}" Foreground="Gray" Margin="0,0,10,0"/>
                                            <Border Background="#E0E0E0" CornerRadius="5" Padding="5,2">
                                                <TextBlock Text="{Binding Category}" Foreground="#424242" FontSize="10" FontWeight="Medium"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1" Style="{StaticResource AmountStyle}" Margin="20,0"/>

                                    <Button Grid.Column="2"
                                            Content="Delete"
                                            Tag="{Binding Id}"
                                            Click="DeleteExpenseButton_Click"
                                            Background="#F44336"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Padding="10,5"
                                            Margin="5,0"/>

                                    <Button Grid.Column="3"
                                            Content="Edit"
                                            IsEnabled="False" 
                                            Background="#9E9E9E"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Padding="10,5"
                                            Margin="5,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2" 
                Content="Close" 
                Width="100" 
                Height="35" 
                HorizontalAlignment="Right"
                Margin="0,20,0,0"
                Background="#757575"
                Foreground="White"
                BorderThickness="0"
                Click="CloseButton_Click"/>
    </Grid>
</Window>