<Window x:Class="TaskForge.WPF.SubscriptionSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:TaskForge.WPF.Converters"
        xmlns:vm="clr-namespace:TaskForge.WPF.ViewModels"
        Title="Керування Підписками" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        Loaded="Window_Loaded">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ваші Підписки" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <Button x:Name="AddSubscriptionButton"
                    Content="Додати підписку"
                    Height="40"
                    Width="150"
                    FontSize="14"
                    Background="#1976D2"
                    Foreground="White"
                    BorderThickness="0"
                    Command="{Binding AddSubscriptionCommand}"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,15">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <TextBlock Text="Список активних підписок" FontSize="18" FontWeight="Medium" Margin="0,10,0,10"/>

            <ListView x:Name="SubscriptionsListView"
                      ItemsSource="{Binding Subscriptions}"
                      Visibility="{Binding IsSubscriptionsListVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                      Background="White"
                      BorderThickness="1"
                      BorderBrush="#DDDDDD"
                      HorizontalContentAlignment="Stretch">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Foreground="Gray" Margin="0,0,15,0">
                                            <Run FontWeight="SemiBold" Text="Сплатити:"/>
                                            <Run Text="{Binding BillingDate, StringFormat='{}{0:dd/MM/yyyy}'}"/>
                                        </TextBlock>
                                        <Border Background="#E3F2FD" CornerRadius="5" Padding="5,2">
                                            <TextBlock Text="{Binding RecurrenceInterval}" Foreground="#1E88E5" FontSize="10" FontWeight="Medium"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="Активне сповіщення" FontSize="10" FontWeight="Bold" Foreground="#388E3C" Margin="0,5,0,0"
                                               Visibility="{Binding Notify, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="20,0">
                                    <TextBlock Text="{Binding Amount, StringFormat='-{0:F2}'}" FontSize="16" FontWeight="Bold" Foreground="#388E3C" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Currency}" FontSize="16" FontWeight="Bold" Foreground="#388E3C" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                </StackPanel>

                                <Button Grid.Column="2"
                                        Content="Редагувати"
                                        Background="#388E3C"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Padding="10,5"
                                        Margin="10,0,5,0"
                                        Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Path=DataContext.OpenEditSubscriptionCommand}"
                                        CommandParameter="{Binding Id}"/>

                                <Button Grid.Column="3"
                                        Content="Видалити"
                                        Background="#F44336"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Padding="10,5"
                                        Margin="5,0,0,0"
                                        Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Path=DataContext.DeleteSubscriptionCommand}"
                                        CommandParameter="{Binding Id}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <Button Grid.Row="2" 
                Content="Закрити" 
                Width="100" 
                Height="35" 
                HorizontalAlignment="Right"
                Margin="0,20,0,0"
                Background="#757575"
                Foreground="White"
                BorderThickness="0"
                Command="{Binding CloseCommand}"/>

        <Grid x:Name="AddSubscriptionModalOverlay" 
              Grid.RowSpan="3"
              Visibility="{Binding IsAddSubscriptionModalVisible, Converter={StaticResource BoolToVisibilityConverter}}"
              Background="#80000000" 
              Panel.ZIndex="100">
            <Border Background="White" 
                    CornerRadius="10" 
                    Padding="30" 
                    Width="400" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Додати Підписку" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                    <TextBlock Text="Назва підписки:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding AddSubscriptionName, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="Вартість:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding AddSubscriptionAmount, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="Дата оплати:" Style="{StaticResource LabelStyle}"/>
                    <DatePicker SelectedDate="{Binding AddSubscriptionBillingDate}" Height="30" Margin="0,0,0,15"/>

                    <TextBlock Text="Інтервал повторення:" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBox Text="{Binding AddSubscriptionIntervalValue, UpdateSourceTrigger=PropertyChanged}" Width="70" Height="30" Padding="5" TextAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding IntervalUnits}" 
                                  SelectedItem="{Binding AddSubscriptionIntervalUnit}" 
                                  Width="100" Height="30"/>
                    </StackPanel>

                    <TextBlock Text="Валюта:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Currencies}" 
                              SelectedItem="{Binding AddSubscriptionCurrency}" 
                              Height="30" Margin="0,0,0,15"/>

                    <CheckBox Content="Нагадувати про оплату" 
                              IsChecked="{Binding AddSubscriptionNotify}" 
                              Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Зберегти" Width="80" Height="35" Margin="0,0,10,0" Command="{Binding SaveAddSubscriptionCommand}" Background="#1976D2" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                        <Button Content="Скасувати" Width="80" Height="35" Command="{Binding CancelAddSubscriptionCommand}" Background="#9E9E9E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="EditSubscriptionModalOverlay" 
              Grid.RowSpan="3"
              Visibility="{Binding IsEditSubscriptionModalVisible, Converter={StaticResource BoolToVisibilityConverter}}"
              Background="#80000000" 
              Panel.ZIndex="101">
            <Border Background="White" 
                    CornerRadius="10" 
                    Padding="30" 
                    Width="400" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Редагувати Підписку" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                    <TextBlock Text="Назва підписки:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding EditSubscriptionName, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="Вартість:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding EditSubscriptionAmount, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="Дата оплати:" Style="{StaticResource LabelStyle}"/>
                    <DatePicker SelectedDate="{Binding EditSubscriptionBillingDate}" Height="30" Margin="0,0,0,15"/>

                    <TextBlock Text="Інтервал повторення:" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBox Text="{Binding EditSubscriptionIntervalValue, UpdateSourceTrigger=PropertyChanged}" Width="70" Height="30" Padding="5" TextAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding IntervalUnits}" 
                                  SelectedItem="{Binding EditSubscriptionIntervalUnit}" 
                                  Width="100" Height="30"/>
                    </StackPanel>

                    <TextBlock Text="Валюта:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Currencies}" 
                              SelectedItem="{Binding EditSubscriptionCurrency}" 
                              Height="30" Margin="0,0,0,15"/>

                    <CheckBox Content="Нагадувати про оплату" 
                              IsChecked="{Binding EditSubscriptionNotify}" 
                              Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Зберегти" Width="80" Height="35" Margin="0,0,10,0" Command="{Binding SaveEditSubscriptionCommand}" Background="#1976D2" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                        <Button Content="Скасувати" Width="80" Height="35" Command="{Binding CancelEditSubscriptionCommand}" Background="#9E9E9E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>